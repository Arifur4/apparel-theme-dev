<!DOCTYPE html>
<html>
  <head>
    {{ content_for_header }}
    {{ 'style.css' | stylesheet_tag }}
  </head>

  <body>
    {% render 'header' %}
    {{ content_for_layout }}
    {{ 'script.js' | asset_url | script_tag }}
  </body>
</html>